#include<stdio.h>
#include<stdint.h>

typedef struct 
{   
    size_t row;
    size_t col;
    float *data;
} Mat;

const char *display_hexcolor(uint32_t c)
{
    static char buffer[1 + 8 + 1];
    buffer[0] = '#';
    buffer[1] = hexchar
}

int main(int argc, char const *argv[])
{
    
    return 0;
}
